/* ===== Estatísticas Component ===== */

const Icons = {
  bookOpen: (
    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
      <path d="M2 3h6a4 4 0 0 1 4 4v14a3 3 0 0 0-3-3H2z"/>
      <path d="M22 3h-6a4 4 0 0 0-4 4v14a3 3 0 0 1 3-3h7z"/>
    </svg>
  ),
  users: (
    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
      <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/>
      <circle cx="9" cy="7" r="4"/>
      <path d="M23 21v-2a4 4 0 0 0-3-3.87"/>
      <path d="M16 3.13a4 4 0 0 1 0 7.75"/>
    </svg>
  ),
  calendar: (
    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
      <rect x="3" y="4" width="18" height="18" rx="2" ry="2"/>
      <line x1="16" y1="2" x2="16" y2="6"/>
      <line x1="8" y1="2" x2="8" y2="6"/>
      <line x1="3" y1="10" x2="21" y2="10"/>
    </svg>
  ),
  star: (
    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
      <polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"></polygon>
    </svg>
  ),
  chart: (
    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
      <line x1="18" y1="20" x2="18" y2="10"></line>
      <line x1="12" y1="20" x2="12" y2="4"></line>
      <line x1="6" y1="20" x2="6" y2="14"></line>
    </svg>
  ),
  trendingUp: (
    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
      <polyline points="23 6 13.5 15.5 8.5 10.5 1 18"/>
      <polyline points="17 6 23 6 23 12"/>
    </svg>
  )
};

function Estatisticas({ estatisticas }) {
  const cards = [
    {
      icon: Icons.bookOpen,
      label: 'Total de Livros',
      value: estatisticas?.total_livros ?? 0,
      color: '#2563eb',
      bgGradient: 'linear-gradient(135deg, #eff6ff 0%, #dbeafe 100%)',
      iconBg: '#dbeafe'
    },
    {
      icon: Icons.users,
      label: 'Total de Usuários',
      value: estatisticas?.total_usuarios ?? 0,
      color: '#7c3aed',
      bgGradient: 'linear-gradient(135deg, #f5f3ff 0%, #ede9fe 100%)',
      iconBg: '#ede9fe'
    },
    {
      icon: Icons.calendar,
      label: 'Empréstimos Ativos',
      value: estatisticas?.emprestimos_ativos ?? 0,
      color: '#059669',
      bgGradient: 'linear-gradient(135deg, #ecfdf5 0%, #d1fae5 100%)',
      iconBg: '#d1fae5'
    },
    {
      icon: Icons.star,
      label: 'Livros Avaliados',
      value: estatisticas?.total_avaliacoes ?? 0,
      color: '#d97706',
      bgGradient: 'linear-gradient(135deg, #fffbeb 0%, #fef3c7 100%)',
      iconBg: '#fef3c7'
    }
  ];

  return (
    <main className="fade-in padding-main">
      <div className="content">
        <section className="section-header">
          <h2>Estatísticas da Plataforma</h2>
          <div className="title-underline"></div>
        </section>

        {estatisticas ? (
          <>
            {/* Stats Cards Grid */}
            <div className="stats-grid-modern">
              {cards.map((card, idx) => (
                <div
                  key={idx}
                  className="stat-card-modern"
                  style={{ background: card.bgGradient }}
                >
                  <div className="stat-card-icon-modern" style={{ background: card.iconBg, color: card.color }}>
                    {card.icon}
                  </div>
                  <div className="stat-card-body">
                    <span className="stat-card-label">{card.label}</span>
                    <span className="stat-card-value" style={{ color: card.color }}>{card.value}</span>
                  </div>
                  <div className="stat-card-decoration" style={{ background: card.color }}></div>
                </div>
              ))}
            </div>

            {/* Summary Banner */}
            <div className="stats-summary-banner">
              <div className="stats-summary-icon">{Icons.trendingUp}</div>
              <div className="stats-summary-text">
                <strong>Visão Geral</strong>
                <p>A plataforma conta com <strong>{estatisticas.total_livros}</strong> livros cadastrados{estatisticas.total_usuarios > 0 && <>, <strong>{estatisticas.total_usuarios}</strong> usuários registrados</>}{estatisticas.emprestimos_ativos > 0 && <> e <strong>{estatisticas.emprestimos_ativos}</strong> empréstimos ativos no momento</>}.</p>
              </div>
            </div>
          </>
        ) : (
          <div className="stats-loading">
            <div className="spinner" style={{ margin: '0 auto' }}></div>
            <p>Carregando estatísticas...</p>
          </div>
        )}
      </div>
    </main>
  );
}

export default Estatisticas;
