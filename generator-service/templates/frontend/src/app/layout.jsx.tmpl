import '../App.css'

export const metadata = {
  title: 'Sistema de Biblioteca',
  description: 'Gerado pelo LPS Biblioteca',
}

export default function RootLayout({ children }) {
  return (
    <html lang="pt-BR">
      <body>
        <div id="root">{children}</div>
      </body>
    </html>
  )
}
