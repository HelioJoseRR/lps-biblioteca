from fastapi import FastAPI

app = FastAPI(title="{{ name }} API")

@app.get("/")
def read_root():
    return {"service": "{{ id }}", "status": "running"}

@app.get("/health")
def health_check():
    return {"status": "ok"}
